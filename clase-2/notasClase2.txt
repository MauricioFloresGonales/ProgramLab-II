clase 2(v2)

namespace : entran clases objetos etc, especifican el area de trabajo
(subGrupo de funcionalidad)
si n osotros podemos usar clases metodos o y enumerados se los llama asi:

usin 'nombreDeLaClase';

alias: podemos darle un nombre auxiliar a una clase
using 'alias' = 'nombreAPonerElAlias'


array: Personaje[] personajes = new Personajes[3];
matriz: Personaje[,] personajes = new Personajes[3];


static: es una funcion que se puede usar sin tener creado el objeto de la clase

'numero en valor int'=int.Parse('nuemroAParcear);

int.TryParse('nuemroAParcear', out 'variable donde carga el resultado SI pudo');

out: setea el dato
ref: pasa la referencia del dato

'nombre'.Next('max','min');



- crear un abm y concatenar los datos
-cerar una clase alumno: string nombre,int legajo
crear un arrayt con 10 estudiantes